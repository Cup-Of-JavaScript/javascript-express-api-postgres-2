const { Pool } = require("pg");

exports.pool = new Pool({
    user: "postgres",
    password: "xxx",     // <== Replace with your password.
    database: "banking", // <=== Does this match your database name???
    host: "localhost",
    port: 5432,
  });
